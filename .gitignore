# R Package internals
inst/doc
/doc/
/Meta/

# RStudio files
*.Rproj
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Claude Code / editor local settings (per-developer)
.claude/
.vscode/
CLAUDE.md

# AI / development audit artifacts (kept locally, not versioned)
audit/
CRAN-SUBMISSION

# Documentation and Config
cran-comments.md
IMPORTANT_FILES

# Custom Data and Folders
manual_synthesis
data-raw
app
simulations
web/
papers/

# Tests and Artifacts
# NB tests_save/ is deliberately NOT ignored here: it holds the archived
# conventions suite and must be versioned. It is excluded from the built
# package by ^tests_save$ in .Rbuildignore instead.
SUMMARY_TESTS.xlsx
archive/
metaConvert.Rcheck/
metaConvert_*.tar.gz
# Windows crash artifacts
*.stackdump
